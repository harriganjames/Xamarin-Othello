<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:view="clr-namespace:Othello.Main.View"
             xmlns:control="clr-namespace:Othello.Main.Control"
             x:Class="Othello.Main.View.OldBoardView"
             x:Name="this"
             >
    <!--<control:OthelloBoardControl ItemsSource="{Binding Cells}"
                             BoardBackgroundColor="DarkGreen"
                             >
        <control:OthelloBoardControl.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ContentView>
                        <ContentView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={x:Reference this}, Path=BindingContext.CellTappedCommand}"
                                                  CommandParameter="{Binding}"/>
                        </ContentView.GestureRecognizers>
                        <view:CellView VerticalOptions="Fill"
                               HorizontalOptions="Fill"
                               />
                    </ContentView>
                </ViewCell>
            </DataTemplate>
        </control:OthelloBoardControl.ItemTemplate>
    </control:OthelloBoardControl>-->
</ContentView>