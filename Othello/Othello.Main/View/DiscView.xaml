<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:Aub.Xamarin.Toolkit.CustomViews;assembly=Aub.Xamarin.Toolkit"
             xmlns:view="clr-namespace:Othello.Main.View"
             xmlns:enum="clr-namespace:Othello.Main.Enum"
             x:Class="Othello.Main.View.DiscView"
             x:Name="this"
             >
  <ContentView.Content>
        <Grid x:Name="mainGrid" BindingContext="{x:Reference this}">
            <!--<Grid x:Name="flatGrid" IsVisible="{Binding IsUse, Converter={StaticResource ReverseBoolConveter}}" RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <BoxView Grid.Row="0" Color="White"/>
                <BoxView Grid.Row="1" Color="Black"/>
            </Grid>-->
            
            <!--<Grid IsVisible="{Binding InUse}">
                <custom:EllipseView Color="{Binding TransitionedState, Converter={StaticResource CellStateToColorConverter}, ConverterParameter=reverse}"
                            Margin="1"/>
                <custom:EllipseView Color="{Binding TransitionedState, Converter={StaticResource CellStateToColorConverter}}"
                            Margin="2"/>
            </Grid>-->
        </Grid>
    </ContentView.Content>
</ContentView>